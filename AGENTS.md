# 项目协作规则

## 开发流程

- 功能开发先阅读相关现有代码，提出计划并与用户协商；计划确认后再实现。
- 影响玩法、同步行为、数据表示或架构的必要抉择先询问用户，不擅自决定。
- 用户已确认的选择继续沿用，不重复询问。实现中发现需要改变已确认方案时，先说明原因并协商。

## 架构与范围

- 沿用现有 World / Entity / Component、GameApplication、网络接口、Protobuf 和消息 Handler 结构。
- 不擅自引入其他架构、框架、依赖或调整现有架构。重构建议先与用户协商。
- 参考项目用于理解具体机制，不整体移植参考项目架构。
- 一次只实现已确认的阶段目标，不预先加入后续功能。
- 整理本次涉及的代码与目录，保持项目结构清晰；不借机进行无关的全项目重构、删除或格式化。
- 保留用户已有的未提交改动。

## 代码整洁

- 不新增数据兼容代码、旧协议适配、双版本路径或为未来需求预留的分支。
- 不留下冗余文件、文件夹、临时代码、未使用变量或无用函数。
- 不为仅有一次调用的顺序逻辑额外抽取包装函数，不增加无意义的封装、转发层或嵌套。
- 保留架构要求的生命周期方法、接口实现及现有 Handler 适配边界；这些不属于无意义包装。
- 新函数、类型和字段应有当前需求中的实际用途，不按假设扩展。

## 验证与交付

- Unity 运行测试由用户自行完成，不搭建 Unity Test、测试场景或额外测试工程。
- 可执行现有编译检查、静态检查；不得将编译成功描述为联机或 Unity 运行验证通过。
- 交付时说明实际修改、已执行的检查，以及简短的 Unity 手动验收步骤。
- 不为验证留下额外脚手架或编译产物到源码目录。
- 设计与实施说明优先合并在一份必要的任务文档中，避免重复文档和多层流程目录。
